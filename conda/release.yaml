name: pydantic-to-djmodel-release
channels:
  - nodefaults
  - conda-forge
dependencies:
  - nodejs >=18
  - python 3.12.*
  - poetry >=1.5
  - pip:
    - makim==1.10.0
    # fix issues with distlib
    - paginate
    # fix issues with distlib
    - jsbeautifier
